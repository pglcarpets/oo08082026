// Re-export for simpler imports in components that only need the hook.
export { useSession, AuthProvider } from './AuthProvider'
export type { SessionState, AuthUser } from '@/features/shared/auth/types'
export type { SharedSession } from '@/features/shared/auth/types'



