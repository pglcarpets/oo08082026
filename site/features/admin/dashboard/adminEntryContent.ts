/** Admin entry hero copy + media — first landing moment inside the authenticated admin shell. */
export const ADMIN_ENTRY_HERO_CONTENT = {
  kicker: "Oando Admin",
  title: "Publish trusted inventory. One record, every operator.",
  subtitle:
    "Confirm identity, footprint, and price before a product reaches Site or Planner — the admin workspace is the single source of truth behind every quote.",
  cta: "Enter workspace",
  meta: "Admin publishes \u2192 Site informs \u2192 Planner designs",
} as const;

/** Graded admin entry hero still (video loops removed). */
export const ADMIN_ENTRY_HERO_MEDIA = {
  poster: "/assets/marketing/hero/admin-entry-poster.webp",
  alt: "Systematic rows of verified office workstations in an Oando install",
} as const;
