"use client";
import { TopBarShell } from "@planner/components/ui/PlannerTopBarShell";

/** Floor Planner top bar — units live on the canvas overlay, not here. */
export const TopBar = () => <TopBarShell productLabel="Floor Planner" />;

export default TopBar;
