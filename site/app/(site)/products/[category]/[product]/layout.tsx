/* Products CSS included once via site/index.css → products/index.css. */

export default function ProductDetailLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}
