"use client";

/**
 * Alias of AdminField — kept for call sites that opted into FOCSS early.
 * Prefer `@/features/admin/ui/AdminFormFields` for new work.
 */
export { AdminField as AdminFocssField } from "./AdminFormFields";

